
node{
     stage('git checkout')
     		{
		git credentialsId: 'git-creds', url: 'https://github.com/Higourang/YGS-Friday'	
	     }

	stage('Maven Package'){
			////Get maven home path and Package
			def mvnHome = tool name: 'Maven-3.6.1', type: 'maven'
                        def mvnCMD = "${mvnHome}/bin/mvn"
			sh "${mvnCMD} clean package"
	     }
      
     }
